{
  "name": "open-meeting-scribe",
  "version": "1.0.0",
  "description": "Chrome extension for Google Meet that records audio and generates structured meeting notes using OpenAI, DeepSeek, LM Studio, Ollama, or any OpenAI-compatible endpoint.",
  "private": true,
  "scripts": {
    "generate-icons": "node scripts/generate-icons.js",
    "setup": "node scripts/generate-icons.js",
    "package": "node scripts/package.js",
    "lint": "echo 'No linter configured — add eslint if desired.'"
  },
  "devDependencies": {
    "sharp": "^0.33.0"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "chrome-extension",
    "google-meet",
    "openai",
    "meeting-notes",
    "transcription",
    "manifest-v3"
  ],
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/open-meeting-scribe"
  }
}
